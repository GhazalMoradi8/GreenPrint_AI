
# requirements.txt

# Core application framework
streamlit

# LlamaIndex Components (pinned to match Colab env)
llama-index-core==0.12.29
llama-index-llms-huggingface==0.5.0
llama-index-embeddings-huggingface==0.5.3
llama-index-readers-file==0.4.7

# Hugging Face libraries
sentence-transformers==2.2.2
transformers
huggingface_hub

# ML framework (often required by transformers/sentence-transformers)
torch

# Utilities used in code or commonly needed
python-dotenv
gdown

# Ensure necessary dependencies for the above are included
# (pip/uv should handle transitive dependencies, but being explicit doesn't hurt)
# Example: Jinja2, numpy, requests etc. are usually pulled in automatically.



